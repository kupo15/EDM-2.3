function enumGOTO() {}
ini_screen_changeGOTO();

enum screen {
appStartup = -1,
eventSetup,
membersList,
stats,
blank3,
blank4,
blank5,


help_info,
settings,
enumcount,

home,

profileCreate,
profileView,

edit_date,
}

enum offsetScroll {
navbarYoff,
numpadYoff,
entryYoff,

sidebarXoff,

memberSortUnderline,

membersOffset,
calendarOffset,

enumcount,
}


enum highlightAction {
courseSelect,	
navbarSwitch
}

enum currencySymbol {

dollar,
euro,
peso,
}

enum textboxEntry {
dispName,	
	
popupEntryText,
}

enum navbar {
enumstart = -5,
calendar = -4,
calendarYearSelect = -3,
main = -2,
hidden = -1,

sidebar,
numpad,

profileChange,
popupEntry,
}

enum member_sort {
abc,
favorite,
firstName,
lastName,
active,
}

