function RoundHistory() constructor {
	
	roundDate = date_current_datetime();
	includedIndex = false; // included in index calculation
	
	differential = 0;
	differentialAdjusted = 0;
	
	teeData = {};
	}

function debug_generate_round_history(arr) {

	var historyStruct = debug_round_history();

	for(var i=0;i<array_length(arr);i++) {
		
		var roundHistory = arr[i].roundHistory;
		var memberDetails = arr[i].memberDetails;
		var lastName = memberDetails.lastName;
		
		
		}

	}

function debug_round_history() {
	
	
	}
